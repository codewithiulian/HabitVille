export interface CameraState {
  x: number;
  y: number;
  zoom: number;
}

export interface Velocity {
  x: number;
  y: number;
}

export interface TouchPoint {
  id: number;
  x: number;
  y: number;
}
